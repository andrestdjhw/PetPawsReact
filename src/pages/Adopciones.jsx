import { useState } from "react"
import { useNavigate } from "react-router";

export const Adopciones = () =>{
    return (
        <section className="container">
        <h1 className="title">Adopciones</h1>    
        </section>
    )
}